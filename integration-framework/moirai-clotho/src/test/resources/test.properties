server.port=8008
spring.h2.console.enabled=true
kairos.cors.enabled=true
kairos.cors.api.allowed-origins=*

kairos.clotho.check-env=false

kairos.neptune.queryoffset=10000

# Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
#spring.datasource.url=jdbc:postgresql://localhost:5432/ryan
#spring.datasource.username= ryan
#spring.datasource.password= password
# The SQL dialect makes Hibernate generate better SQL for the chosen database
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update