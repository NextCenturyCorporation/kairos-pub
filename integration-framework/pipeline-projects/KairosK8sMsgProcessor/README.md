# KairosK8sMsgProcessor

# Docker-compose 
Run `docker-compose -f docker-redis.yml up -d;`
after that is up successfully
Run `docker-compose up`

# Build
Run `./gradlew`

# Deploy to ecr
Run `./gradlew deploy` after building